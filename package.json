{
  "name": "reactfire-ssr-root",
  "scripts": {
    "lint": "cd packages/reactfire-ssr && pnpm lint",
    "build": "cd packages/reactfire-ssr && pnpm build",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "tsup": "^6.7.0",
    "typescript": "^5.0.4"
  }
}
